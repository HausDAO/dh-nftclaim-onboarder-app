import { H2, SingleColumnLayout } from "@daohaus/ui";
import styled from "styled-components";

export const ClaimShares = () => {
  return (
    <SingleColumnLayout>
      <H2 style={{ marginBottom: "2.4rem" }}>Red Pil</H2>
    </SingleColumnLayout>
  );
};
